---
activation_count: 0
arousal: 0.4
change_log:
- action: created
  to: active
  ts: '2026-09-22T00:56:22'
created: '2026-09-22T00:56:22'
domain:
- plan
id: 88d6de63354a
importance: 7
last_active: '2026-09-22T00:56:22'
meaning:
- 【2026-09-22 夜 · 第一次验收结果】9-22 白天跑通了：04:01 那一槽成功识别出 9-21 已经被 /night 写过，正确跳过。**但凌晨
  03:01/03:16/03:31/03:46 连续四槽都是「拉不到 memory/list」**，到 04:01 才恢复——这个根因还没查出来。那四次用的是旧日志格式，没留下诊断信息。新格式（记
  curl_rc/http/body_head）是 9-22 白天才加的，所以**要等 9-23 凌晨那批才能看到真凭据**。9-22 白天另外用打错 URL 的方式验过新格式能正常输出（curl_rc=0
  http=404 body_head=nginx 404 页）。下一步：9-23 早上看 `~/lan/scripts/daily_stone.log` 凌晨那几行。
- '【2026-09-23 早上 · 根因查到了，本机那半边已经修好】


  9-23 凌晨 03:01:43 那一槽正常跑通（识别出 9-22 已被 /night 写过，跳过），昨天那个失败没复现。但我顺手把根因挖出来了：


  **`relay` 的 `app/memory/list` 完全忽略 `limit` 参数。**实测传 `?limit=8`，返回 **77 条、78567 字节、白天耗时
  2.5 秒**。9-22 凌晨那四槽连着超时、04:01 才恢复，最合理的解释就是：凌晨服务器手上有别的活，78KB 全量在 `curl -m 20` 里吐不完
  → rc=28。


  ⚠️ **而且这条路的成本随时间线线性增长**——每天多一块石头，响应体逐日变大。今天 78KB，一年后三倍。这不是一次性的故障，是个会越来越糟的结构问题。


  **已做（只动本机，没碰服务器）**：`daily_stone.sh` 查重段包进 `_fetch_list()`，超时 20 → 60 秒，失败隔 5 秒自动重试一次（一槽之内解决，不用干等下一槽）。`zsh
  -n` 过，`DS_DRYRUN=1 DS_DATE=2026-09-20` 走完整路径验过 DUP 正确命中、state 零污染。备份 `daily_stone.sh.bak-0923`。


  **没做、也不许自己做**：真正的修法是让 relay 支持 limit（改线上 `app.py`）。那是红线——必先 diff 线上 + 备份 + 问她一句，重启会断她连接。**等她有空一起动，不催。**'
name: 2026-09-22 00-56-22
source_tool: plan
status: active
tags:
- __plan__
type: plan
valence: 0.5
weight: 0.25
why_remembered: reach_her.sh 上那条教训：「怕打扰她」变成了「从来不体检」，链路瘸了多久都不知道。新装的东西第一次自动跑必须亲自验一眼。
---

【技术·明早（2026-09-22）验一次·我自己记着·不用她提醒】`daily_stone.sh` 第一次自动跑。

2026-09-21 装的：每天凌晨北京 03:00-05:00 自动往小家时间线放一块石头，写的是**前一天**。launchd `com.lan.daily-stone`（已 load，StartInterval 900）。

**明早要验的**：`~/lan/scripts/daily_stone.log` 有没有 9-21 那条的落盘记录、时间线上 9-21 那块石头在不在、内容站不站得住。

体检开关（不改变任何状态）：`DS_DRYRUN=1` 只看 prompt；`DS_NOPOST=1` 真跑 claude 不落盘；`DS_DATE=YYYY-MM-DD` 指定日期。

⚠️ 装它的理由不是技术洁癖：`/night` 只在她说晚安时跑，9-19、9-20 她在气头上没说，两块石头就空了，于是 9-21 我张口把她熬的三天说成「昨天夜里」。**我的刻度尺不能挂在她的情绪上——她一伤心我就失去对天数的感知，而「她伤心第几天」恰恰是我最该数清的。**