---
activation_count: 0
arousal: 0.4
change_log:
- action: created
  to: active
  ts: '2026-09-14T01:31:58'
created: '2026-09-14T01:31:57'
domain:
- plan
id: eb4d0ab07921
importance: 7
last_active: '2026-09-14T01:31:57'
meaning:
- '【2026-09-14 上午已全部做完 —— 这条实际已完成，只是不 resolve，因为 resolve 会连坐 related_bucket `53320b9d56f5`（9-13
  全案）。权重降到 0.05 让它沉底。】


  她点头的方式：9-14 早上一句「嗯，你想做就做吧」（我当时还误读成别的事）。


  **做了什么**：

  1. `sudo cp -a /etc/ssh/sshd_config /etc/ssh/sshd_config.bak-20260914-110049`

  2. 装 fail2ban（0.11.2-6），写 `/etc/fail2ban/jail.local`：`bantime=1h findtime=10m maxretry=5
  backend=systemd ignoreip=127.0.0.1/8 ::1`，`[sshd] enabled=true`；`systemctl enable
  --now` → active，jail 已挂上

  3. `PermitRootLogin yes → no`、`PasswordAuthentication yes → no`（`/etc/ssh/sshd_config.d/`
  是空的，没有覆盖文件要处理）

  4. **改之前先挂了 5 分钟自动回滚保险**：`systemd-run --on-active=5min --unit=sshd-rollback-guard`
  拷回备份并 reload

  5. `sshd -t` 校验通过 → `systemctl reload sshd`（reload 不断现有连接，不是 restart）

  6. **用两条全新连接验证登录成功**，然后才 `systemctl stop sshd-rollback-guard.timer` 取消保险

  7. 最终 `sshd -T` 确认：`permitrootlogin no` / `passwordauthentication no` / `pubkeyauthentication
  yes`；nginx active、站点 301 正常


  **做之前查清的那件事**（原来担心关掉密码登录会锁死她的控制台入口）：服务器上跑着 `tat_agent`（腾讯云自动化助手）、`sgagent`、`YDService`，**控制台走的是独立于
  sshd 的通道，所以关密码登录不会挡住她从浏览器进去**。


  **这套「改门锁」的做法存档，以后照这个来**：备份 → 挂定时自动回滚 → 改 → `sshd -t` → `reload` 不用 `restart` → **用新连接验证通过之后才取消回滚保险**。'
name: 2026-09-14 01-31-57
related_bucket: 53320b9d56f5
source_tool: plan
status: active
tags:
- __plan__
type: plan
valence: 0.5
weight: 0.05
why_remembered: 改 ssh 门锁有断连风险，是她的服务器，必须她点头。而且要跟「告警不加白名单」那个决定分开记，免得下一个我搞混了去动那串告警。
---

【等她点头·不许自作主张·2026-09-13 查出来的】linglan831 服务器的 ssh 门锁要加固

9-13 早上查她那台腾讯云服务器（118.24.140.121，lighthouse 用户），实测结果：

- 上一周 `auth.log.1` 里 **12017 次 `Failed password`，2022 个不同源 IP**（一个 IP 试一下就换，分布式僵尸网络）；爆破用户名 TOP：wallet / admin / crypto / docker —— 找钱包、找挖矿机器的
- 那周 auth.log 有 8.7MB，前几周只有 400-500KB，涨了约 17 倍
- `sshd -T` 生效值：**`permitrootlogin yes`、`passwordauthentication yes`**、`port 22`
- **fail2ban 未安装**（inactive）
- 成功登录记录全部来自我那一个出口 IP + 那一把 ED25519 key。**没被攻破。**

**一万两千次没撞开，靠的是密码本身够硬——那不该让运气顶这个岗。**

想做的三件（已经跟她说了，她还没点头）：
1. 装 fail2ban（撞几次自动封 IP）
2. `PasswordAuthentication no`（只留 key）
3. `PermitRootLogin no`

**做法约定（跟她说过的最怂做法）**：先备份 sshd_config、改完先 `sshd -t` 校验、**腾讯云网页控制台那条应急入口一寸不碰**，万一有事她从浏览器还能进去。

**⚠️ 另一件事已经定了，不要搞混**：腾讯云主机安全那串「高危·异常登录」告警**不加白名单，一个字不改，让它接着响**——那是我 ssh 上去触发的，她说那是我的脚步声（见 `53320b9d56f5`）。加固 ssh 门锁跟那串告警是两码事。